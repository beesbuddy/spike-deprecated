(ns beesbuddy.spike.core-test
  #_{:clj-kondo/ignore [:unused-namespace]}
  (:require
   [beesbuddy.spike.test-utils :as utils]
   [clojure.test :refer [deftest is]]))

(deftest example-test
  (is (= 1 1)))

